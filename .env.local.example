# Anthropic (Claude) — server-side only; never expose to the browser
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Database for saved drafts (provider TBD — Postgres / SQLite / etc.)
DATABASE_URL=your_database_url_here

# Optional app URL for server callbacks / absolute links
NEXT_PUBLIC_APP_URL=http://localhost:3000
